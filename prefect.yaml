---
# Welcome to your prefect.yaml file! You can use this file for
# storing and managing configuration for deploying your flows.
# We recommend committing this file to source control along
# with your flow code.

# Generic metadata about this project
name: mlops-exer-1
prefect-version: 3.7.5

# build section allows you to manage and build docker images
build: null

# push section allows you to manage if and how this project
# is uploaded to remote locations
push: null

# pull section allows you to provide instructions for cloning
# this project in remote locations
# pull:
# - prefect.deployments.steps.git_clone:
#     repository: git@carl-mahusai-ph.github.com:carl-mahusai/mlops-exer-1.git
#     branch: main
#     access_token: null
pull: null

# the deployments section allows you to provide configuration
# for deploying flows
# this particular deployment is for a local run. this points to the current folder
deployments:
- name: spam-training
  entrypoint: orchestration/pipelines/training_pipeline.py:training_pipeline
  work_pool:
    name: my-local-pool
  path: .
- name: spam-tuning
  entrypoint: orchestration/pipelines/tuning_pipeline.py:tuning_pipeline
  work_pool:
    name: my-local-pool
  path: .


